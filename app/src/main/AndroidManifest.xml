<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sushant.udacityproject3_curiousgooner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:largeHeap="true">
        <activity android:name=".StartScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:theme="@style/MyTheme"></activity>
        <activity android:name=".ScreenTwoActivity"
            android:theme="@style/MyTheme"></activity>
        <activity android:name=".ScreenThreeActivity"
            android:theme="@style/MyTheme"></activity>
        <activity android:name=".ScreenFourActivity"
            android:theme="@style/MyTheme"></activity>
        <activity android:name=".ScreenFiveActivity"
            android:theme="@style/MyTheme"></activity>
        <activity android:name=".ScreenSixActivity"
            android:theme="@style/MyTheme"></activity>
        <activity android:name=".ScreenSevenActivity"
            android:theme="@style/MyTheme"></activity>
        <activity android:name=".FinalScreenActivity">
            </activity>
    </application>

</manifest>
